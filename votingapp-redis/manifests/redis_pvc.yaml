apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: redisclaim
  namespace: prod
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: redisstor
